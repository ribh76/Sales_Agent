# SalesCompass Frontend

Next.js app for running ICP analyses, reviewing results, submitting human feedback, and comparing the agent against a baseline.

## Run Locally

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` to the FastAPI base URL. The default is `http://localhost:8000/api/v1`.

