[project]
name = "salescompass-backend"
version = "0.1.0"
description = "FastAPI backend for the SalesCompass ICP AI agent"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"

